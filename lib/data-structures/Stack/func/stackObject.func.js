
const generateItems = _ => new Object()
const getItem = (items, key) => items[ key ]
const setItem = (items, key, value) => items[ key ] = value

export {
    generateItems,
    getItem,
    setItem
}
